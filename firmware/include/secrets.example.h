#pragma once

// Copy this file to secrets.h and fill in your WiFi credentials.
// secrets.h is gitignored.
//
// Leave WIFI_SSID empty to disable WiFi entirely at runtime (or define
// WIFI_DISABLED in platformio.ini build_flags to disable at compile time).

#define WIFI_SSID     ""
#define WIFI_PASSWORD ""
